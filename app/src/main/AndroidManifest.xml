<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.MusicStructureApp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.android.MusicStructureApp.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name="com.android.MusicStructureApp.PlayingActivity" android:label="@string/playing"/>
        <activity android:name="com.android.MusicStructureApp.AboutActivity" android:theme="@style/AppTheme.ActionBar" android:label="@string/about"/>
        <activity android:name="com.android.MusicStructureApp.PaymentActivity" android:theme="@style/AppTheme.ActionBar" android:label="@string/buy"/>
    </application>

</manifest>